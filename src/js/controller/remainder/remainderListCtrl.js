angular
    .module('crmApp')
    .controller('remainderListCtrl', ['$scope', 'callFactory' , '$state', function ($scope, callFactory, $state) {
        $scope.data = {}
        $scope.options = {}
        $scope.handlers = {
        
        }

        init();

        function init() {
           
        }

       
    }]);